#include "gtest/gtest.h"
#include "src/thread/exception_handler.hpp"

#include <string>

using std::string;

using namespace error_handler;

TEST(failure_handle, default_error_condition)
{
		/*
		failure_handler<int>  failure_handle;
		int thread_no_file = 100;
		failure_handle.default_error_condition(thread_no_file);
		*/
};


 

